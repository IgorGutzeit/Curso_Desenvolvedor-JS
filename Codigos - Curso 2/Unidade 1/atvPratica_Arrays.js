let arr = [1, 2, 3, 4, 5];
let soma = 0;

for (let element of arr){
    soma += element;
};

console.log(soma);


//Exibindo Arrays com estruturas de repetição simples
for (let i = 0; i < arr.length; i++){
    console.log(arr[i]);
};